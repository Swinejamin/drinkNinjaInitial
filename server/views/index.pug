extends ./layout.pug
block content
    script.
        var user = globalUser;
        var userData = firebase.database().ref('users/' + user);
    .container#index

    script(src='../../public/main.js' type='text/javascript')